apiVersion: v2
name: prometheus-grafana
description: Prometheus + Grafana stack for homelab
type: application
version: 1.0.0

dependencies:
  - name: kube-prometheus-stack
    version: "28.0"        # Pin to a specific minor version
    repository: https://prometheus-community.github.io/helm-charts